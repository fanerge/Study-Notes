适配器模式（adapter）、包装器（wrapper）
1.定义：适配器模式（Adapter）是将一个类（对象）的接口（方法或属性）转化成客户希望的另外一个接口（方法或属性）
2.生活例子：适配器模式的作用就像一个转换器，本来手机不能直接连接电脑，通过使用接口就可以了。
3.使用场景：使用一个已经存在的对象，但其方法或属性接口不符合你的要求；
4.例子：库A有一个$id方法，你想用jquery库来实现。
	$id = function( id ){
		return jquery('#' + id)[0];
	};





















